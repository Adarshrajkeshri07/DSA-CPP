#include<iostream>
using namespace std ; 
int main(){ 
    long long  int a ;
    cout << sizeof( a )<< endl;
    cout << sizeof(short);
    return 0 ;
    //int 4 byte
    // long int 4 byte in window .and 8 byte in mac or linux  . 
    // for human age or other short number we use - short -  that have 2 byte  . that is enough for  age and year and day etc  ......

    // all int double long and other are by default signed its store positive and negative both type of number .
    // we also use unsigned that store only positive number like age , id , balance, people, and other positive ......

}